{
  "type": "module",
  "scripts": {
    "start": "node --experimental-specifier-resolution=node src/cli.js -p 'yarn ava test/massive.test.js --tap' -p 'yarn ava test/massive.test.js --tap'",
    "test": "cat <(ava test/massive.test.js --tap) <(ava test/massive.test.js --tap) | tap-merge | tap-nyan"
  },
  "dependencies": {
    "multistream": "^4.1.0",
    "tap-merge": "^0.3.1",
    "tap-nyan": "github:LKay/tap-nyan",
    "yargs": "^17.2.0"
  },
  "devDependencies": {
    "ava": "^3.15.0"
  }
}
